[project]
name = "moudo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cython>=3.0.12",
    "pyinstaller>=6.12.0",
    "typer>=0.15.2",
]
